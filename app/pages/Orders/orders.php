<div class="dash-content">
    <div class="overview">
        <div class="title">
            <i class="uil uil-tachometer-fast-alt"></i>
            <span class="text"><?= $title ?></span>
        </div>
        <div class="activity">
            <div class="activity-data">

                <div class="data names">
                    <span class="data-title">id</span>
                    <!-- <?php foreach ($data['orders'] as $order) : ?>
                        <span class="data-list"><?= htmlspecialchars($order->id) ?></span>
                    <?php endforeach; ?> -->
                </div>
                <div class="data email">
                    <span class="data-title">User</span>
                    <!-- <?php foreach ($data['orders'] as $order) : ?>
                        <span class="data-list"><?= htmlspecialchars($order->image) ?></span>
                    <?php endforeach; ?> -->
                </div>
                <div class="data email">
                    <span class="data-title">Total Amount</span>
                    <!-- <?php foreach ($data['orders'] as $order) : ?>
                        <span class="data-list"><?= htmlspecialchars($category->name) ?></span>
                    <?php endforeach; ?> -->
                </div>

                <div class="data email">
                    <span class="data-title">Date</span>
                    <!-- <?php foreach ($data['orders'] as $order) : ?>
                        <span class="data-list"><?= htmlspecialchars($category->name) ?></span>
                    <?php endforeach; ?> -->
                </div>

                <div class="data email">
                    <span class="data-title">Status</span>
                    <!-- <?php foreach ($data['orders'] as $order) : ?>
                        <span class="data-list"><?= htmlspecialchars($category->name) ?></span>
                    <?php endforeach; ?> -->
                </div>
              
              
                <form method="post">
                    <div class="data status">
                        <span class="data-title">Operation</span>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>